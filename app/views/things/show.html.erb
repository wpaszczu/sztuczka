<%- model_class = Thing -%>


<div id="carousel-example-generic" class="carousel slide col-md-6 fotos" data-ride="carousel">
  <!-- Indicators -->
  <ol class="carousel-indicators">
    <li data-target="#carousel-example-generic" data-slide-to="0" class="active"></li>
    <li data-target="#carousel-example-generic" data-slide-to="1"></li>
    <li data-target="#carousel-example-generic" data-slide-to="2"></li>
  </ol>

  <!-- Wrapper for slides -->
  <div class="carousel-inner" role="listbox">
    <div class="item active">
      <%= image_tag @thing.foto.url(:medium), :class => 'img-responsive' %>
    </div>
    <div class="item">
      <%= image_tag @thing.foto2.url(:medium), :class => 'img-responsive' %>
    </div>
    <div class="item">
      <%= image_tag @thing.foto3.url(:medium), :class => 'img-responsive' %>
    </div>

  </div>

</div>





<div class="col-md-6">



  <div class="panel-body"><h2><%= @thing.title %></h2></div>

  <div class="panel-body"><%= simple_format(@thing.opisik) %></div>

<div class="btn btn-default btn-circle btn-xl poka1 "><%= model_class.human_attribute_name(:opis) %></div>
<div class="btn btn-default btn-circle btn-xl poka2"><%= model_class.human_attribute_name(:meta) %></div>
<div class="btn btn-default btn-circle btn-xl poka3"><%= model_class.human_attribute_name(:recenzja) %></div>

  <div class=" znik1"><%= @thing.opis %></div>

  <div class=" znik2"><%= simple_format(@thing.meta) %></div>

  <div class=" znik3"><%= @thing.recenzja %></div>


</div>

<!--
<%= link_to t('.back', :default => t("helpers.links.back")),
              things_path, :class => 'btn btn-default btn-circle btn-xl'  %>
<%= link_to t('.edit', :default => t("helpers.links.edit")),
              edit_thing_path(@thing), :class => 'btn btn-default btn-circle btn-xl' %>
<%= link_to t('.destroy', :default => t("helpers.links.destroy")),
              thing_path(@thing),
              :method => 'delete',
              :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
              :class => 'btn btn-danger btn-circle btn-xl' %> -->
</div> <!-- koniec col-md-6 -->
